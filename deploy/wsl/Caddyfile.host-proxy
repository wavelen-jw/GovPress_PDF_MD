:8080 {
    encode gzip zstd

    @health path /health
    handle @health {
        reverse_proxy 127.0.0.1:8013
    }

    handle /v1/* {
        reverse_proxy 127.0.0.1:8013
    }

    handle {
        respond "GovPress API gateway is running." 200
    }
}
