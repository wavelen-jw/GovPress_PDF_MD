# 2026-04-05 Work Log

## Summary

Today focused on two tracks:

1. Stabilizing the report/HWPX pipeline by reducing repeated structure interpretation.
2. Fixing the PDF path for report documents, especially `260121 AI시대 공공문서 혁신 추진계획_수정`, where HWPX was already close to the golden markdown but PDF was not.

Current latest deployed commit on `web` and VPS:

- `e24b689` `fix: normalize pdf report extraction before markdown routing`

VPS status at end of day:

- branch/head: `e24b689`
- service: `govpress-api.service active`
- health: `{"status":"ok"}`

## Architecture State

### HWPX path

Current structure is much cleaner than before:

1. extract in [src/hwpx_converter.py](/home/wavel/GovPress_PDF_MD/src/hwpx_converter.py)
2. HWPX-specific normalization in [src/hwpx_postprocessor.py](/home/wavel/GovPress_PDF_MD/src/hwpx_postprocessor.py)
3. one-time document type detection via [src/markdown_postprocessor.py](/home/wavel/GovPress_PDF_MD/src/markdown_postprocessor.py)
4. type formatter, especially [src/report_postprocessor.py](/home/wavel/GovPress_PDF_MD/src/report_postprocessor.py)
5. thin finalizer / spacing cleanup

Important result:

- HWPX no longer re-enters the generic markdown router and gets reclassified multiple times.

### PDF path

Current PDF path:

1. extract via `opendataloader` in [src/converter.py](/home/wavel/GovPress_PDF_MD/src/converter.py)
2. run `_normalize_pdf_raw_text()` in the same file
3. pass normalized text into `postprocess_markdown()`
4. report formatter / finalizer

Important result:

- PDF still uses `opendataloader`, but now has a PDF-only normalization step before report formatting.

## Main Fixes Done Today

### 1. PDF report normalization added

Implemented in [src/converter.py](/home/wavel/GovPress_PDF_MD/src/converter.py).

Added:

- page number removal like `- 1 -`
- ASCII roman heading recovery:
  - `I` + `추진배경` -> `Ⅰ. 추진배경`
  - same for `II`, `III`, `IV`
- inline structure splitting:
  - `< 기존 문서 AI 활용 >`
  - `< 앞으로의 문서 >`
  - `< 가이드라인 예시 >`
  - `< 추진경과 >`
  - `<개 요>`
- PDF task icon normalization:
  - `` -> `󰋎/󰋏/󰋐`
- line cleanup for common PDF extraction artifacts:
  - `글` -> `ᄒᆞᆫ글`
  - `마크 다운` -> `마크다운`
  - `더 라도` -> `더라도`
  - `모델 (VLM)` -> `모델(VLM)`

### 2. PDF flattened table repair added

Also in [src/converter.py](/home/wavel/GovPress_PDF_MD/src/converter.py).

Added PDF-specific rewrites for:

- `현황 및 문제점` grouped recognition table
  - one compressed row -> multiple markdown table rows
- duplicated compressed summary task table under section III
  - remove duplicate compressed version before formatter
- compressed `변화관리 수단(안)` table
  - recover `동기부여 / 조직문화 / 역량강화` rows
- `붙임1`, `붙임2` title split lines
  - merge into single appendix heading lines

## Validation Results

### Pair comparison in `tests/problem`

Compared local PDF/HWPX pairs in:

- `/home/wavel/GovPress_PDF_MD/tests/problem`

Latest observed HWPX vs PDF similarity:

- `250828_행안부 범정부 초거대 AI 공통기반 설명자료_수정2`: `96.1214%`
- `251011_국정자원 배터리 관련 참고자료_수정`: `87.8354%`
- `260121 AI시대 공공문서 혁신 추진계획_수정`: `93.1200%`
- `260331 (국무회의 종료시) ...`: `98.7366%`

Key change:

- `260121` improved from about `87.2706%` to `93.1200%` for PDF vs HWPX after the PDF normalization work.

### Notes on `260121`

Interpretation now:

- HWPX path is already close to the intended/golden markdown.
- PDF path was the wrong one and was corrected today.

Remaining differences for `260121` are mostly:

- some compressed complex tables still flatter on PDF than on HWPX
- some appendix example blocks are still more degraded on PDF
- minor wording/spacing differences coming from extraction, not formatter intent

## Files Most Relevant Tomorrow

- [src/converter.py](/home/wavel/GovPress_PDF_MD/src/converter.py)
- [src/report_postprocessor.py](/home/wavel/GovPress_PDF_MD/src/report_postprocessor.py)
- [src/markdown_postprocessor.py](/home/wavel/GovPress_PDF_MD/src/markdown_postprocessor.py)
- [tests/run_problem_regression.py](/home/wavel/GovPress_PDF_MD/tests/run_problem_regression.py)

## Suggested Next Steps

### Priority 1

Keep improving PDF-only reconstruction for report PDFs, but do not touch HWPX rules unless regression is proven.

### Priority 2

Use `260121` as the main report PDF regression target:

- compare PDF output against HWPX output
- then against golden markdown once the golden file is present locally

### Priority 3

If continuing PDF work, likely next targets are:

- compressed appendix example blocks in `붙임1`
- `추진체계` / `향후일정` compressed tables and schedule layout

## Important Operational Notes

- Local branch may be behind remote `web` and worktree is not clean. When pushing a focused change, using a temp clone + `git format-patch` / `git am` remains safer than rebasing the main worktree directly.
- VPS deploy method currently used successfully:
  - SSH to `wavel@223.130.139.45:2222`
  - reset `/home/wavel/GovPress_PDF_MD` to `origin/web`
  - restart `systemctl --user govpress-api.service`

